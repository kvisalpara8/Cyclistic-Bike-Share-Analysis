-- Create database

--CREATE DATABASE cyclistic_project

-- Create table

CREATE TABLE annual_2023_data (
    ride_id VARCHAR(255),
    rideable_type VARCHAR(255),
    started_at TIMESTAMP,
    ended_at TIMESTAMP,
    member_casual VARCHAR(255)
)